{
  "name": "karv-cloud-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env staging",
    "types:generate": "wrangler types worker-configuration.d.ts --env-interface CloudflareBindings --include-runtime false --strict-vars false",
    "types:check": "wrangler types worker-configuration.d.ts --env-interface CloudflareBindings --include-runtime false --strict-vars false --check",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:smoke": "node scripts/smoke-test.mjs",
    "test:admin-hardening": "node --test test/admin-hardening/*.test.mjs",
    "audit:security": "npm audit --audit-level=high",
    "check": "npm run types:check && npm run typecheck && npm run test",
    "check:admin-hardening": "node --check scripts/cloudflare-admin-audit.mjs && node --check scripts/cloudflare-ai-gateway-configure.mjs && node --check scripts/scan-added-lines-for-secrets.mjs && node --check test/admin-hardening/mock-cloudflare-fetch.mjs && node --check test/admin-hardening/admin-hardening.test.mjs && npm run test:admin-hardening",
    "dry-run:staging": "wrangler deploy --env staging --dry-run --outdir dist/staging",
    "dry-run:production": "wrangler deploy --env production --dry-run --outdir dist/production",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260801.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10",
    "wrangler": "4.118.0"
  },
  "overrides": {
    "undici": "7.29.0"
  },
  "engines": {
    "node": ">=22"
  }
}
